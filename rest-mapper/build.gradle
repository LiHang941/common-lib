publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.github.lihang941'
            artifactId = 'rest-mapper'
            version = '0.0.1'
            from components.java
        }
    }
}

dependencies {   
    compile "io.vertx:vertx-web:$vertxVersion"
}
